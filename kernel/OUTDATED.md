# Kernel outdated points

This status file has no open outdated items after the current source audit.

| Former item | Resolution |
| --- | --- |
| Syscall inventory count | Updated in the kernel overview, syscall overview, and syscall prototypes page. |
| `vfork()` semantics | Documented in `kernel/proc/forking.md` and `kernel/proc/stacks.md`. |
| Process groups and sessions | Documented in `kernel/syscalls/proc.md` and summarized in `kernel/proc/forking.md`. |
| Signal handling details | Documented in `kernel/syscalls/signals.md`. |
| Thread states and flags | Documented in `kernel/proc/forking.md`. |
| File I/O and socket boundary | Documented in `kernel/syscalls/index.md` and `kernel/syscalls/file.md`. |

Reopen this file when a source update makes a reader-facing kernel page stale.
